#描述
计算圆的弦长、弧长、拱高、半径的工具，已知两个参数可以算出另外两个。
双击calculate.html就可以进入主界面。
#describe
Calculate the circle length, arc length, arch height and the radius of tool, the two known parameters can be calculated in two.
